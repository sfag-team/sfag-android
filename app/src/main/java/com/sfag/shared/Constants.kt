package com.sfag.shared

/** MIME types for opening JFF files via SAF. */
val JFF_OPEN_MIME_TYPES = arrayOf("application/octet-stream", "text/xml")

/** MIME type for creating/saving JFF files via SAF. */
const val JFF_SAVE_MIME_TYPES = "application/octet-stream"

/** Intent extra key for example asset path. */
const val EXTRA_EXAMPLE_URI = "example uri"

/** Intent extra key for example display name. */
const val EXTRA_EXAMPLE_NAME = "example name"
